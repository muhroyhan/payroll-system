// Placeholder root screen — replaced by the real dashboard in FE-T32
// (09_FRONTEND_STEPS.md). Exists so the router has a route to mount at FE-T01.
export function HomePage() {
  return <div>Payroll System — Admin</div>;
}
