// §5.1 — affects TER calculation method (monthly TER vs TER Harian).
export enum EmploymentStatus {
  TETAP = "tetap",
  TIDAK_TETAP = "tidak_tetap",
}

export enum EmployeeActiveStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
}
